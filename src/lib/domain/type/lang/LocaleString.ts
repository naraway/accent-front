class LocaleString {
  locale: string = '';
  string: string = '';

  constructor(locale: string, string: string) {
    //
    this.locale = locale;
    this.string = string;
  }
}

export default LocaleString;

